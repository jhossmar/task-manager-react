import React from 'react';

function Header(): React.JSX.Element {
    return (
        <header id='Title'>
            <h1>Task Manager</h1>
        </header>
    );
}

export default Header;