function EmptyState() {
  return (
    <div className="empty-state-container">
      <p className="empty-state-text"> No hay tareas todavía. Agrega una nueva tarea.</p>
    </div>
  );
}

// Asegúrate de que termine en punto y coma (;) y no en dos puntos (:)
export default EmptyState;